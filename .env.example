# API.md 2절 서버 주소 기준
VITE_API_BASE_URL=http://localhost:8080/api/v1
VITE_BACKEND_ORIGIN=http://localhost:8080

# true면 workspace/ai-review/understanding-card/agreement-log를 mock으로 처리
# 실제 API 배포되면 false로 바꾸고 각 feature의 api/mock -> api 실제 구현으로 교체
VITE_USE_MOCK=true
